<%= content_tag :div,
  id: 'card-form',
  data: {
    card: @card.to_json(except: [:created_at, :updated_at])
  } do %>
  <label>Card name</label>
  <input type="text" v-model="card.title" />
<% end %>
